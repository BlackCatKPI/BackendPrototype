This is a model of backend project. It can add, remove, update and delete users and their roles from DB using REST interface.

1. Create MySQL shema named "prototypebd"
2. populate shema with SQL Query "Database population.sql". It adds user and roles.
3. Root user 
	login: Supervis0r
	password: p4s5w0rd
